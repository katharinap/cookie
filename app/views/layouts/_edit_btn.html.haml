- remote ||= false
- if disabled ||= false
  = link_to '#', disabled: true do
    = glyph :pencil
- else
  = link_to path, title: t('.edit', default: t("helpers.links.edit")), data: { toggle: 'tooltip' }, remote: remote do
    = glyph :pencil
